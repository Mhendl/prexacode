# Nada dentro de data/ debe servirse por HTTP: contiene la base de leads y logs.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
